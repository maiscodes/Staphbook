#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../include-html/bin/include" "$@"
  ret=$?
else 
  node  "$basedir/../include-html/bin/include" "$@"
  ret=$?
fi
exit $ret
