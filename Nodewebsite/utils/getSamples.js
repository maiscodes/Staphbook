const path = require("path");
const fs = require("fs");

/**
 * Given an array of samples names, return required data.
 * Uses: Groups/Favourites
 * @param {string[]} sampleNames
 * @returns {object[]} samples
 */
function getSamples(sampleNames){

}
